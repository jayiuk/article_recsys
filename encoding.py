class encoding:
    def __init__(self, input, encoder):
        self.input = input
        self.encoder = encoder

    def vocab(input):
        word_list = []
        for word in input:
            word = word.lower()
            word_list.append(word)
        return vocab
    