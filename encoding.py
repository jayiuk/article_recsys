class encoding:
    def __init__(self, input, encoder):
        self.input = input
        self.encoder = encoder

    def encoding(input, encoder):
        word_list = []
        for word in input:
            word = word.lower()
            word_list.append(word)
        return encoded
    