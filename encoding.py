class encoding:
    def __init__(self, input, encoder):
        self.input = input
        self.encoder = encoder

    def encoding(input, encoder):
        for word in input:
            word = word.lower()
            
        return encoded
    